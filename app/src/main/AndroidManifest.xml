<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.company.jetbizcard">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.JetBizCard">
        <activity
            android:name=".JetTipApp"
            android:exported="true"
            android:label="@string/title_activity_jet_tip_app"
            android:theme="@style/Theme.JetBizCard.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".IntroToCompose"
            android:exported="true"
            android:label="@string/title_activity_intro_to_compose"
            android:theme="@style/Theme.JetBizCard.NoActionBar">
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.JetBizCard.NoActionBar"></activity>
    </application>

</manifest>